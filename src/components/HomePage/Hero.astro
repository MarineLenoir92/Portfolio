<section class="hero">
  <div class="hero-content">
    <h1>MARINE LENOIR</h1>
    <p class="subtitle">DÉVELOPPEUSE FRONT-END</p>
    <p class="specialty">SPÉCIALISÉE EN ACCESSIBILITÉ NUMÉRIQUE</p>
  </div>
  
  <div class="hero-image">
    <picture>
      <source media="(max-width: 768px)" srcset="/Visuel-Home-Mobile.webp" />
        <img
        src="/Visuel-Home-Desktop.webp"
        alt="Bureau minimaliste avec un ordinateur affichant le logo ML, un carnet et une plante." 
        loading="eager"
        />
    </picture>
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr;
    min-height: 80vh;
    align-items: center;
    background-color: var(--bg-color);
    text-align: center;
  }

  .hero-content {
    padding: 4rem 2rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    order: 1;
  }

  h1 {
    font-size: 2rem;
    font-weight: 400;
    letter-spacing: 0.1em;
    margin-bottom: 2rem;
    color: var(--primary);
  }

  .subtitle {
    font-size: 1.25rem;
    letter-spacing: 0.05em;
    line-height: 2.3rem;
    margin-bottom: 0.5rem;
    color: var(--text-main);
  }

  .specialty {
    font-size: 1.25rem;
    letter-spacing: 0.05em;
    line-height: 2.3rem;
    color: var(--text-main);
  }

  .hero-image {
    order: 2;
    height: 40vh;
    width: 100%;
  }

  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }


  @media (min-width: 64rem) {
    .hero {
        display: flex;
        height: calc(100vh - 100px);
        width: 100%;
    }

    .hero-content {
        padding: 0 5%;
        order: 0;
        text-align: left;
    }

    .hero-image {
        order: 0;
        flex: 1;
        height: 100%;
    }

    .hero-image :global(img) {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
    }

    h1 {
        font-size: 3rem;
        margin-bottom: 1.5rem;
    }

    .subtitle, .specialty {
        font-size: 1.5rem;
        line-height: 2.813rem;
    }
  }
</style>